language: c++
compiler: gcc
dist: trusty

addons:
  apt:
    packages:
    - e2fsprogs
    - uuid
    - uuid-dev
    - gnome-common
    - libparted-dev
    - libgtkmm-3.0-dev
    - libdevmapper-dev

script:
 - ./autogen.sh
 - make
